# `pipeline edit` opens pipeline YAML in editor

Opens the YAML definition file of a pipeline in your configured editor. The file
must exist locally. Select a pipeline by ID or filter by keywords.

## Examples

- `ado pipeline edit 12345` - edit by pipeline ID
- `ado pipeline edit buddy` - search by name
- `ado pipeline edit` - interactive selection
