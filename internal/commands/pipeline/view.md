# `pipeline view` opens pipeline in browser

Opens the pipeline build page in the browser. Provide a pipeline ID or keywords
to filter by name. If multiple pipelines match, an interactive picker is shown.

## Examples

- `ado pipeline view 12345` - view by pipeline ID
- `ado pipeline view buddy` - search by name
- `ado pipeline view` - interactive selection
