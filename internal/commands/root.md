# `ado` CLI - Azure DevOps Command Line Tool

Fast, friendly CLI to work with Azure DevOps seamlessly from your terminal.

To getting started, `cd` into an ADO repo, then `ado config init`. See
`help config`, `help pr` and other help topics to learn more.
