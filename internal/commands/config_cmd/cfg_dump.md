# `config dump` shows resolved configuration

Prints the fully resolved configuration as YAML after processing `include!`
file(s), environment variables, command-line flags, and auto-detected settings
based on working dir. This is useful for debugging configuration issues.
