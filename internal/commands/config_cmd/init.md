# `config init` creates a new config file

Create a new `.ado.yaml` with default values and documentation. If a `.config`
directory exists at the repo root, you can choose between placing the file there
or at the repo root.
